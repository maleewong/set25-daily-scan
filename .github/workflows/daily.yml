name: daily-report

on:
  schedule:
    - cron: "0 21 * * *"     # 04:00 น. เวลาไทย
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    env:
      KERNEL_NAME: gha-py311

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt

      - name: Register Jupyter kernel in this venv
        run: |
          python -m ipykernel install --name "${KERNEL_NAME}" --sys-prefix
          jupyter kernelspec list

      # รัน notebook โดยตรง + ซ่อน code แล้วเซฟเป็น docs/index.html
      - name: Execute notebook -> HTML (hide code)
        run: |
          mkdir -p docs
          jupyter nbconvert \
            --to html \
            --execute \
            --ExecutePreprocessor.kernel_name="${KERNEL_NAME}" \
            --ExecutePreprocessor.timeout=1800 \
            --TemplateExporter.exclude_input=True \
            WFO_test_no_out.ipynb \
            --output "index.html" \
            --output-dir docs

      - name: Upload Pages artifact
        uses: actions/upload-pages-artifact@v3
        with:
          path: docs

  deploy:
    needs: build
    runs-on: ubuntu-latest
    permissions:
      pages: write
      id-token: write
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    steps:
      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4

      # ส่งอีเมลแจ้งลิงก์เว็บหลัง deploy สำเร็จ
      - name: Prepare 7-day window text
        id: dates
        run: |
          echo "START_DATE=$(date -u -d '7 days ago' +'%Y-%m-%d')" >> $GITHUB_ENV
          echo "END_DATE=$(date -u +'%Y-%m-%d')" >> $GITHUB_ENV

      - name: Send report email
        if: ${{ success() }}
        uses: dawidd6/action-send-mail@v3
        with:
          server_address: smtp.gmail.com
          server_port: 587
          secure: true
          username: ${{ secrets.SMTP_USERNAME }}
          password: ${{ secrets.SMTP_PASSWORD }}   # Gmail App Password
          subject: "SET25 daily scan ${{
            env.END_DATE
          }}"
          to: ${{ secrets.MAIL_TO }}               # ถ้าอยาก hardcode: maleewong@gmail.com
          from: SET25 Bot <${{ secrets.SMTP_USERNAME }}>
          body: |
            สรุปผลสแกนหุ้น SET25 ช่วง 7 วันล่าสุด: ${{ env.START_DATE }} – ${{ env.END_DATE }}
            ดูรายงาน: ${{ steps.deployment.outputs.page_url }}



